<Window x:Class="TodoApp.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard"
        Width="640"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}"
        Icon="/Assets/app.ico"
        FontFamily="Segoe UI">

    <Window.Triggers>

        <EventTrigger RoutedEvent="FrameworkElement.Loaded">

            <BeginStoryboard>

                <Storyboard>

                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        To="1"
                        Duration="0:0:0.2"/>

                </Storyboard>

            </BeginStoryboard>

        </EventTrigger>

    </Window.Triggers>

    <StackPanel Margin="28,24,28,24">


        <!-- ================================================= -->
        <!-- Header -->
        <!-- ================================================= -->

        <TextBlock Text="Dashboard"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"/>

        <TextBlock x:Name="FocusSummaryText"
                   Text="{Binding FocusSummary}"
                   FontSize="12.5"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,4,0,20"/>


        <!-- ================================================= -->
        <!-- Stat cards -->
        <!-- ================================================= -->

        <ItemsControl ItemsSource="{Binding StatCards}"
                      Margin="0,0,0,26">

            <ItemsControl.ItemsPanel>

                <ItemsPanelTemplate>

                    <UniformGrid Columns="4"/>

                </ItemsPanelTemplate>

            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>

                <DataTemplate>

                    <Border Background="{StaticResource CardBrush}"
                            CornerRadius="10"
                            Padding="16,14"
                            Margin="0,0,12,0">

                        <StackPanel>

                            <TextBlock Text="{Binding Value}"
                                       FontSize="26"
                                       FontWeight="Bold"
                                       Foreground="{Binding Accent}"/>

                            <TextBlock Text="{Binding Label}"
                                       FontSize="11.5"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Margin="0,3,0,0"/>

                        </StackPanel>

                    </Border>

                </DataTemplate>

            </ItemsControl.ItemTemplate>

        </ItemsControl>


        <!-- ================================================= -->
        <!-- Completed this week chart -->
        <!-- ================================================= -->

        <TextBlock Text="COMPLETED THIS WEEK"
                   FontSize="11"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextMutedBrush}"
                   Margin="0,0,0,10"/>

        <Border Background="{StaticResource CardBrush}"
                CornerRadius="10"
                Padding="18,14"
                Margin="0,0,0,26">

            <ItemsControl ItemsSource="{Binding WeekBars}">

                <ItemsControl.ItemsPanel>

                    <ItemsPanelTemplate>

                        <UniformGrid Columns="7"/>

                    </ItemsPanelTemplate>

                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <StackPanel HorizontalAlignment="Center" Margin="0,0,8,0">

                            <Grid Height="130"

                                  VerticalAlignment="Bottom">

                                <Border
                                    VerticalAlignment="Bottom"
                                    Width="34"
                                    CornerRadius="5,5,2,2"
                                    Height="{Binding BarHeight}">

                                    <Border.Style>

                                        <Style TargetType="Border">

                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>


                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding IsToday}" Value="True">

                                                    <Setter Property="Background" Value="#51CF66"/>

                                                </DataTrigger>

                                            </Style.Triggers>

                                        </Style>

                                    </Border.Style>

                                </Border>

                            </Grid>

                            <TextBlock
                                Text="{Binding Count}"
                                FontSize="11"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                HorizontalAlignment="Center"
                                Margin="0,6,0,2"/>

                            <TextBlock
                                Text="{Binding DayLabel}"
                                FontSize="11"
                                Foreground="{StaticResource TextMutedBrush}"
                                HorizontalAlignment="Center"/>

                        </StackPanel>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </Border>


        <!-- ================================================= -->
        <!-- Category breakdown -->
        <!-- ================================================= -->

        <TextBlock Text="BY CATEGORY"
                   FontSize="11"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextMutedBrush}"
                   Margin="0,0,0,10"/>

        <ItemsControl ItemsSource="{Binding CategoryStats}">

            <ItemsControl.ItemTemplate>

                <DataTemplate>

                    <Border Background="{StaticResource CardBrush}"
                            CornerRadius="8"
                            Padding="14,10"
                            Margin="0,0,0,8">

                        <Grid>

                            <Grid.ColumnDefinitions>

                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>

                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">

                                <Grid>

                                    <Grid.ColumnDefinitions>

                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>

                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Name}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding PercentText}"
                                               FontSize="12"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource AccentBrush}"/>

                                </Grid>

                                <Border x:Name="CategoryTrack"
                                        Height="6"
                                        CornerRadius="3"
                                        Background="{StaticResource InputBrush}"
                                        Margin="0,7,0,0">

                                    <Border HorizontalAlignment="Left"
                                            CornerRadius="3"
                                            Background="{StaticResource AccentBrushDark}">

                                        <Border.Width>

                                            <MultiBinding Converter="{StaticResource PercentOfTrackConverter}">

                                                <Binding Path="Percent"/>

                                                <Binding ElementName="CategoryTrack"
                                                         Path="ActualWidth"/>

                                            </MultiBinding>

                                        </Border.Width>

                                    </Border>

                                </Border>

                            </StackPanel>

                            <TextBlock Grid.Column="1"
                                       Margin="14,0,0,0"
                                       VerticalAlignment="Center">

                                <Run Text="{Binding Completed, Mode=OneWay}"

                                     Foreground="#51CF66"

                                     FontSize="12"/>

                                <Run Text="/" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>

                                <Run Text="{Binding Total, Mode=OneWay}"

                                     Foreground="{StaticResource TextMutedBrush}"

                                     FontSize="12"/>

                            </TextBlock>

                        </Grid>

                    </Border>

                </DataTemplate>

            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </StackPanel>

</Window>
